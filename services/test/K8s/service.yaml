apiVersion: v1
kind: Service
metadata:
  name: udecrypt-image-service
spec:
  selector:
    app: udecrypt-image-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
